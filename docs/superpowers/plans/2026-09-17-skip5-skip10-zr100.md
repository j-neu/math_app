# skip5_forward/backward_zr100 + skip10_forward/backward_zr100 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the remaining 4 skills of BUILD_ORDER.md Batch 1.4 (skip-counting) playable: `skip5_forward_zr100`, `skip5_backward_zr100`, `skip10_forward_zr100`, `skip10_backward_zr100`. This closes out all 8 Batch 1.4 skills. Zero new Dart code — sub-batch 1.4b (`docs/superpowers/plans/2026-09-17-skip2-zr100.md`) already built the shared `hundred_chart_skip` widget/generator generically over `step ∈ {2, 5, 10}`; this plan is pure spec-authoring, re-scaling the same param shape for `step: 5` and `step: 10`.

**Architecture:** Identical structure to `skip2_forward_zr100`/`skip2_backward_zr100`: levels 1-2 use `hundred_chart_skip` (field-visible, `visible_count: 3` then `2`), level 3 uses `sequence_gap` (mental, field hidden). Only `step` and the `start_range` windows change per skill, re-derived so every value stays in `[1, 100]`.

**Tech Stack:** Flutter/Dart (`math_app/`), the v4 skill-spec JSON pipeline (`_generateHundredChartStep`, `HundredChartStepWidget`, `_generateSequenceGap`, `SequenceGapWidget` — all reused unchanged from sub-batch 1.4b), Python sync/coverage scripts.

**Spec:** `docs/clean-room/v4/skills/BUILD_ORDER.md` Batch 1.4 (lines 40-43) + `docs/superpowers/specs/2026-09-15-exercise-plan-design.md` lines 177-183 ("skip5_forward/backward_zr100 ... Example: '15, 20, 25, ...?'"; "skip10_forward/backward_zr100 ... Example: '30, 40, 50, ...?'"). Precedent: `docs/superpowers/plans/2026-09-17-skip2-zr100.md` (sub-batch 1.4b, the widget-building plan this one extends).

## Global Constraints

Identical to `docs/superpowers/plans/2026-09-17-skip2-zr100.md`'s Global Constraints section (ZR100 `[1,100]` bounds, `step ∈ {2,5,10}` enforced by the generator, `error_taxonomy` is `miscount` + `other` only, domain `A`, `problem_count: 8`/`mastery.correct_of: 8`) — not repeated here since no new code or evaluation behavior is introduced.

---

## Task 1: All 4 spec JSONs + tests

**Files:**
- Create: `docs/clean-room/v4/skills/specs/skip5_forward_zr100.json`, `skip5_backward_zr100.json`, `skip10_forward_zr100.json`, `skip10_backward_zr100.json`
- Test: `math_app/test/skill_spec_store_test.dart`, `math_app/test/problem_generators_test.dart`

**Interfaces:**
- Consumes: `hundred_chart_skip` / `sequence_gap` (both unchanged, from sub-batch 1.4b).
- Produces: 4 real specs.

- [x] **Step 1: Write all 4 spec JSONs.** Bound checks (`direction: up` -> max `= start_range[1] + step*visible_count`; `direction: down` -> min `= start_range[0] - step*visible_count`; all must land in `[1,100]`):
  - `skip5_forward_zr100`: L1 `start_range:[10,40]`, `visible_count:3`, `step:5` (max `40+15=55`); L2 `start_range:[40,70]`, `visible_count:2` (max `70+10=80`); L3 `sequence_gap` `start_range:[10,50]`, `length:4`, `gap_indices:[3]` (max `50+15=65`).
  - `skip5_backward_zr100`: L1 `start_range:[60,90]`, `visible_count:3` (min `60-15=45`); L2 `start_range:[30,60]`, `visible_count:2` (min `30-10=20`); L3 `start_range:[50,90]` (min `50-15=35`).
  - `skip10_forward_zr100`: L1 `start_range:[10,40]`, `visible_count:3`, `step:10` (max `40+30=70`); L2 `start_range:[30,60]`, `visible_count:2` (max `60+20=80`); L3 `start_range:[10,40]` (max `40+30=70`).
  - `skip10_backward_zr100`: L1 `start_range:[60,90]`, `visible_count:3` (min `60-30=30`); L2 `start_range:[40,70]`, `visible_count:2` (min `40-20=20`); L3 `start_range:[40,80]` (min `40-30=10`).
  All within `[1,100]`; all `gap_indices:[3] < length:4`.
- [x] **Step 2: Add store tests** — parametrized over the 4 skill ids in a `for` loop (matching the size of this batch rather than hand-writing 4 near-identical blocks), each asserting `domain == 'A'`, the level-template list `['custom_widget','custom_widget','sequence_gap']`, and `customWidget == 'hundred_chart_skip'` on levels 1-2.
- [x] **Step 3: Add generator tests** — also parametrized, over a `{id: (step, forward)}` map, each pair producing an L1/L2 arithmetic+range test and an L3 arithmetic+range test (8 tests total for 4 skills).
- [x] **Step 4: Run** `flutter test test/skill_spec_store_test.dart test/problem_generators_test.dart` — PASS. Store: 19/19 (up from 15). Generator: 174/174 (up from 166).
- [x] **Step 5: Commit** — `feat(skip5,skip10): add v4 skill specs for the remaining 4 Batch 1.4 skills`.

---

## Task 2: Sync, coverage, BUILD_ORDER, full suite

**Files:**
- Modify: 4 files under `math_app/assets/skill_specs/` (generated)
- Modify: `docs/clean-room/v4/skills/BUILD_ORDER.md`

- [x] **Step 1: Sync** — `synced 15 skill specs ... (4 copied, 11 unchanged)`.
- [x] **Step 2: Coverage check** — `covered: 15/93`.
- [x] **Step 3: Update BUILD_ORDER.md** — check off all 4 remaining Batch 1.4 lines (all 8 skills of the batch now done), add all 4 to `## Done`, correct the trailing count to "these fifteen."
- [ ] **Step 4: Run the full Flutter test suite** — expect PASS.
- [ ] **Step 5: Run `flutter analyze`** — expect no new issues beyond the 341 baseline.
- [ ] **Step 6: Commit** — `chore(skip5,skip10): sync spec assets, mark Batch 1.4 fully done in BUILD_ORDER`.

---

## Self-Review Notes

- **Spec coverage:** all 4 remaining Batch 1.4 skills covered; Batch 1.4 (8/8 skills) is now fully shipped across 3 sub-batches (1.4a: zr20 pair via `numberline_step`+`sequence_gap`; 1.4b: zr100 proving pair + the new `hundred_chart_skip` widget; 1.4c: this plan, the remaining 4 zr100 skills as pure reuse).
- **Placeholder scan:** none.
- **Type consistency:** identical field names/types to sub-batch 1.4b's tests (`visible_values`/`query_value` as `List<int>`/`int`), confirmed by the parametrized tests passing against all 4 new real specs.
- **Why parametrized (`for`-loop) tests instead of 4 hand-written blocks:** with 4 near-identical skills differing only in `step`/`direction`, a loop keeps the test file from ballooning with copy-pasted blocks while still exercising every skill by its real id — consistent with "Files that change together should live together" but applied to test structure once the pattern is proven (sub-batch 1.4b's 2 skills were still hand-written, since that was the first use of the widget and each assertion's exact shape needed to be visible for review).
